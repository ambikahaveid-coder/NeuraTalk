name: neuratalk_sdk
description: Official Dart/Flutter SDK for the NeuraTalk API (real-time translated calling, transcripts, webhooks).
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.2
  crypto: ^3.0.5

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
