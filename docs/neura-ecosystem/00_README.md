# NEURA Ecosystem Architecture — Phase 0

**Status:** Phase 0 (Audit + Architecture) complete. No implementation started under this initiative.
**Date:** 2026-08-23
**Source of truth:** Six parallel codebase audits against `c:\Users\kiran\Downloads\Neura-Talk (1)\Neura-Talk` at commit `0b875c0`, cited with file:line evidence throughout.

## Why this folder exists separately

`docs/architecture/`, `docs/audit/`, and `docs/roadmap/` already contain a large number of prior audit and roadmap documents (`FINAL_ZERO_ASSUMPTION_AUDIT.md`, `MASTER_PRODUCTION_READINESS_CHECKLIST.md`, `PRODUCTION_READINESS_CERTIFICATION.md`, `MASTER_ROADMAP_INDEX.md`, and others). That volume is itself a finding: it's evidence of a repeated pattern in this project — audit gets written, roadmap gets written, implementation doesn't fully follow through, next audit gets written. See `12_PRODUCTION_RISK_REGISTER.md`, risk R18.

This folder is scoped narrowly to the current ecosystem-transformation initiative so it doesn't get lost in or mistaken for that prior body of documents. It supersedes prior architecture docs only where they conflict with the evidence cited here; it does not delete or invalidate them.

## Documents in this initiative

| # | File | Purpose |
|---|---|---|
| 1 | `01_CURRENT_STATE_ARCHITECTURE.md` | What exists today, evidence-classified |
| 2 | `02_TARGET_NEURA_ARCHITECTURE.md` | The ecosystem target, as a module tree |
| 3 | `03_MESSAGING_CONSOLIDATION_PLAN.md` | Collapsing 3 parallel messaging systems into 1 |
| 4 | `04_CALL_ARCHITECTURE_CONSOLIDATION_PLAN.md` | Collapsing overlapping call-session models into 1 |
| 5 | `05_AI_MODEL_GATEWAY_ARCHITECTURE.md` | Single AI abstraction layer, replacing 13+ ad hoc provider calls |
| 6 | `06_AI_SECURITY_AND_PERMISSION_MODEL.md` | READ/SUGGEST/CONFIRM/EXECUTE/HIGH-RISK tiering for every AI action |
| 7 | `07_RAG_AND_MEMORY_ARCHITECTURE.md` | Knowledge retrieval + permission-scoped memory (currently missing) |
| 8 | `08_E2E_PRIVACY_ARCHITECTURE.md` | Honest current-state (not E2E) + 4-mode privacy design |
| 9 | `09_BUSINESS_PLATFORM_ARCHITECTURE.md` | Business profiles, catalog, templates, campaigns, ads, commerce, agents |
| 10 | `10_MASTER_MIGRATION_ROADMAP.md` | Dependency-ordered phase sequence |
| 11 | `11_ARCHITECTURE_DECISION_RECORDS.md` | ADRs for the load-bearing decisions in this initiative |
| 12 | `12_PRODUCTION_RISK_REGISTER.md` | Top risks, P0–P3, with owner-facing tracking columns |
| 13 | `13_BUSINESS_PLATFORM_MASTER_AUDIT.md` | Multi-channel (WhatsApp/SMS-DLT/Email) adapters, white-label tenancy, template/campaign/automation engines, event catalog — extends 01-12, doesn't replace them |
| 14 | `14_NEURA_BUSINESS_SAAS_SPECIFICATION.md` | Standalone B2B SaaS spec (clone the *product category*, not Meta's tech) — corrects 13's external-approval mirroring since there's no Meta dependency at all; full domain model, API map, screen inventory, phased plan |
| 18 | `18_ADMIN_AND_BUSINESS_SAAS_IMPLEMENTATION_AUDIT.md` | Admin auth, super-admin, business admin, tenant architecture, RBAC, security/billing audit, Meta-model mapping, P0-P3 findings |
| 19 | `19_ADMIN_FORMS_CRUD_GOVERNANCE_MASTER_AUDIT.md` | Full lifecycle (create→approve→suspend→delete→audit) audit of every admin form/entity; corrects/sharpens 18's tenant finding (a real `organizations`-based business CRUD exists, just unnamed as "tenant"); master entity matrix + P0-P3 implementation plan |
| 35 | `35_PHASE8B_MARKETING_CONTROL_CENTER_ARCHITECTURE.md` | Phase 8B: READ-ONLY audit + design for a Business Marketing Control Center UI, no code written. Fresh frontend audit this turn: shadcn/Radix + react-query + wouter foundation is real and consistent, but zero prior art exists for the three things this console needs most -- a real data table, a shared status badge, and any adopted chart wrapper (recharts used bespoke in one file only). Confirmed zero business-facing UI exists for any Phase 4-8 module. Core finding: reporting/pre-flight-estimate backend gaps (business-wide campaign rollup, cost aggregation, frequency-cap visibility) must be built BEFORE the campaign builder/reports UI, reordering the brief's own suggested implementation sequence with evidence (ADR-3) -- building against fabricated numbers was explicitly rejected. Hard UI rule: the delivery funnel's honest ceiling is SENT, ACCEPTED/DELIVERED render as disabled/labeled, never fabricated (ADR-6). Zero new RBAC permissions proposed; "Manager" role's real shape flagged UNKNOWN, not assumed. Not implemented |
| 34 | `34_PHASE8_MARKETING_HARDENING_IMPLEMENTATION.md` | Phase 8 hardening: closes both P0 gaps from doc 33. Consent write path (POST/GET .../customers/:id/consent) exposes the already-real Phase 4 setChannelConsent/isEligibleForChannel -- zero schema change, opt-out IS a revoke, no second consent mechanism. New atomic DB-backed frequency/throughput counters (customer_marketing_frequency, business_marketing_throughput), campaign-agnostic by design (so overlap/duplicate-suppression falls out for free), proven under genuine Promise.allSettled concurrency against a simulated Postgres unique-violation. Documents the actual (not blindly-copied) execution gate order. 29 new tests, 664/664, zero regressions. Migration written, not applied. Not deployed |
| 33 | `33_PHASE8_MARKETING_MESSAGING_ARCHITECTURE_AUDIT.md` | Phase 8: READ-ONLY audit + architecture for Marketing Messaging, no code written. Core finding: the existing Campaign Engine was already built category-agnostic (`MARKETING` already in `CAMPAIGN_ALLOWED_CATEGORIES`) -- marketing is a policy configuration of what exists, not a new system (ADR-1). Two P0 gaps found, neither a flaw in shipped code: no route exists for a customer to actually grant/revoke marketing consent (the gate is real but unreachable), and no frequency/rate cap exists for marketing sends. Full REAL/PARTIAL/MISSING dependency matrix, threat model, consent/opt-out model with explicitly unresolved product decisions, channel/billing/reporting architecture, UI inventory (confirmed: zero UI exists for any Phase 4-7 module), risk register, implementation order, and 5 ADRs. Not deployed, not implemented |
| 32 | `32_BUSINESS_UTILITY_MESSAGING_IMPLEMENTATION.md` | Phase 7: business utility (operational/service) messaging foundation. Pre-implementation audit found NO existing booking/order/payment event resolving an end-customer -- built as a pure internal receiving abstraction (`triggerUtilityMessage`, no HTTP trigger route at all) rather than fabricating a producer; the one honestly-real candidate (call lifecycle events) is documented as a deliberately-deferred extension point, not silently wired in scope-creep. Marketing-safety boundary is explicit: no NLP content classifier exists or is claimed -- utility/marketing separation is enforced entirely via server-checked template category + human approval, documented as a governance boundary not a content-understanding one. Idempotency (business+eventType+eventReference) is the first in this series proven under a genuinely simulated Postgres unique-violation, not just an app-level pre-check. Only 1 new RBAC permission (UTILITY_VIEW, no send-capable permission exists). Not deployed |
| 31 | `31_BUSINESS_OTP_AUTHENTICATION_IMPLEMENTATION.md` | Phase 6: business OTP/authentication messaging foundation for a business to challenge/verify ITS OWN customers -- parallel to, never merged with, the platform's own login OTP (separate table, Redis key namespace via a parameterized `namespace` arg added to rate-limit.ts). crypto.randomInt + SHA-256 hash, never plaintext, never logged/returned. Resend = supersede (one live challenge at a time), atomic-CAS verify/resend proven under genuine concurrency. Content rendering is split: the only path that persists content redacts `{{code}}` to a fixed marker, structurally incapable of storing the real code. Extracted `campaigns/billing.ts`'s primitive into a shared `billing/message-billing.ts` so OTP billing reuses it, not duplicates it. Consent-exempt by design (documented, not silently assumed). Not deployed |
| 30 | `30_CAMPAIGN_ENGINE_IMPLEMENTATION.md` | Phase 5: campaign orchestration engine (WHO/WHAT/WHEN/authority/consent gates, no external channel). Immutable recipient snapshot at schedule/execute time (never live-re-queried mid-run); template binding fixed to one approved `templateVersions.id`, re-checked live at execution, never "latest". Reuses the Generic Approval Center for campaign approval (no new approval table) and Phase 4's consent/status gates verbatim. New `campaigns/billing.ts` atomic-CAS wallet deduction (placeholder pricing, explicitly not a policy decision) -- not a billing-engine rebuild. Idempotent/concurrency-safe execution proven with genuine `Promise.allSettled` races. Extends Phase 0 messaging to validate `CUSTOMER`-type participants. Not deployed |
| 29 | `29_BUSINESS_CUSTOMERS_AUDIENCES_IMPLEMENTATION.md` | Phase 4: business customer/contact + audience recipient foundation (not a CRM, no sending). `customers` separates global NEURA identity from per-business relationship via an optional `linkedUserId`; deterministic dedup (phone/email/externalRef, business-scoped, never silently merged); `customerConsents` keeps communication eligibility separate from customer existence/status (absence of a row = not eligible). Explicit `audienceMembers` junction (not a JSON array); DYNAMIC audience type declared but rejected, not faked. 4 new RBAC permissions (CUSTOMERS/AUDIENCES x VIEW/MANAGE). Wires Phase 0's `businessConversations.customerId` forward-reference to a real FK. Not deployed |
| 28 | `28_GENERIC_APPROVAL_CENTER_IMPLEMENTATION.md` | Phase 3: domain-neutral approval engine (1 table, code-level policy registry keyed by resourceType -- not a policy table). Templates is the only registered domain. Self-approval enforcement moved OUT of the Template module entirely into this layer (removes the "two independent systems" duplication). Atomic compare-and-swap decide (same pattern as billing-engine's wallet reservation) proven safe under genuine concurrent approve+reject. Not deployed |
| 27 | `27_BUSINESS_TEMPLATE_ENGINE_IMPLEMENTATION.md` | Phase 2: versioned message template engine (DRAFT/SUBMITTED/APPROVED/REJECTED/ARCHIVED), reuses Phase 0's MESSAGE_CATEGORY, 2 new RBAC permissions (not 6) with a service-layer self-approval guard for separation of duties, safe single-pass placeholder rendering (no code execution), proven immutable once approved. No sender-identity fields anywhere (doc 25 compliance). Not deployed |
| 26 | `26_BUSINESS_PROFILE_BRANDING_IMPLEMENTATION.md` | Phase 1: real Business Profile + Branding replacing the disabled settings stub. 6 new columns (legal name, description, structured address), branding stored in existing `settings` jsonb (zero new columns), zero new RBAC permissions (ORG_VIEW/EDIT_SETTINGS already existed and were already correctly scoped). Found + fixed a real file-upload validation gap during implementation. Not deployed |
| 25 | `25_MESSAGE_SENDER_IDENTITY_ARCHITECTURE.md` | Extends 24's P1 sender fix to non-human senders (OTP/utility/campaign/AI/automation/system). Recommends separating conversational identity (existing participantType, no new types needed) from generation provenance (new `generationSource` in messagingEvents.payload, no schema change). 9-use-case trigger tables, Phase-1 integration rules. Design only, not implemented |
| 24 | `24_CANONICAL_MESSAGING_FOUNDATION.md` | Resolves 23 §3's blocking naming decision: 10 new `messaging_`-prefixed tables (Conversation/Message/Participant/Delivery/Attachment/Translation/ReadState/Reaction/Event + BusinessConversation), fully additive, zero changes to the 3 existing chat systems. Full schema, ER diagram, lifecycles, RBAC, API contract, 10 Phase-0 acceptance tests. Design only, not yet approved for implementation |
| 23 | `23_NEURA_BUSINESS_MESSAGING_MASTER_BUILD_PLAN.md` | Full architecture for templates/campaigns/audiences/OTP/utility/marketing/automation/inbox -- built on a canonical Conversation/Message model since the 3 existing chat systems were never consolidated (doc 03); flags that as a blocking Phase 0. 12-phase dependency-ordered build plan, not yet approved for implementation |
| 20 | `20_PRODUCTION_DEPLOYMENT_INTEGRITY_AUDIT.md` | Root-cause: DigitalOcean deployed the committed `dist/` directly, never ran `npm run build` -- every server-touching commit from `6ab722d` through `430c34b` (~22h) was pushed/marked ACTIVE without the runtime artifact ever changing, including the P0 `/api/conversations` security fix. Section 0: the approved Model A fix, implemented |
| 21 | `21_PRODUCTION_DEPLOYMENT_RUNBOOK.md` | Canonical deployment + production-verification procedure going forward: source-is-truth build pipeline, mandatory 5-point verification protocol, rollback steps |

**Visual artifact (current-state + target-state, published earlier this session):** https://claude.ai/code/artifact/444207ba-36c1-4739-8b30-ae16f208719d

## Standing constraints this initiative operates under

- A feature freeze is active: no new application features until the core communication stack (calls, translation, messaging) is physically certified.
- A live translation test (Telugu ↔ English, real devices) is in progress; its result is a hard input to `01_CURRENT_STATE_ARCHITECTURE.md`'s translation classification and is not yet folded in as of this writing.
- **No production code has been touched to produce these documents.** Only this `docs/neura-ecosystem/` folder was created.
- Implementation does not begin until the user has reviewed and approved this architecture, per their explicit instruction.
